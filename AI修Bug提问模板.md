# AI 修 Bug 提问模板

## 不知道文件和函数时

```text
请使用项目中的 AI 维护规则帮我修复一个 Bug。

【问题现象】
{实际表现或完整报错}

【复现步骤】
1. {步骤一}
2. {步骤二}

【预期结果】
{应该发生什么}

【当前环境】
{语言、框架、运行命令或操作系统；不知道可省略}

【要求】
先用搜索定位最可能的文件和函数，然后只读取目标函数、直接依赖、相关配置、数据结构和最小测试。不要扫描或上传整个项目。上下文不足时，请明确告诉我缺少哪个函数、变量、调用栈、复现步骤或测试命令，不要猜测。修复后运行最小相关验证，并记录根因、修改文件和验证结果。
```

## 已知目标函数时

```text
请执行函数级 Bug 修复，并遵守项目根目录的 AGENTS.md。

【问题现象】
{实际表现、完整报错、何时发生}

【复现步骤】
1. {步骤一}
2. {步骤二}

【预期结果】
{原本应该出现的结果}

【目标位置】
文件：{路径}
函数/类/入口：{名称}

【允许读取范围】
- 目标实现
- 目标直接调用的自定义函数
- 使用的全局配置和数据结构
- 相关最小测试

【限制】
- 不扫描或上传整个项目。
- 默认只修改目标函数或目标模块。
- 保留已有注释、函数签名和对外行为。
- 不覆盖或回滚我已有的未提交修改。
- 信息不足时先说明缺少什么，不要猜。

【验证】
修改后运行：{最小测试命令}
并记录根因、修改文件、验证结果和未验证风险。
```

## 新项目创建时

```text
请使用项目初始化规则从零创建一个项目。

【项目目标】
{一句话说明目标}

【核心功能】
1. {功能一}
2. {功能二}
3. {功能三}

【技术偏好】
{语言、框架、数据库、部署方式；没有偏好时请给出少量方案并说明取舍}

【开发规则】
先输出目录结构、模块职责、接口和调用关系，再创建代码。不要把全部功能写进一个文件；配置集中管理；每个关键功能有最小测试；创建 AGENTS.md、ARCHITECTURE.md、BUG_HISTORY.md 和提问模板。实现后运行语法检查、最小测试和启动验证，并说明尚未验证的部分。
```
